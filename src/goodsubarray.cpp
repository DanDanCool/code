#include <stdio.h>

char array[10001];

int main()
{
    int t;
    scanf("%i", &t);

    for (int i = 0; i < t; i++)
    {
        int n;
        scanf("%i", &n);
        scanf("%s", array);
    }
}
